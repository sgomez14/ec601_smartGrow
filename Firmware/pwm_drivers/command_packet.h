// command_packet.h

#ifndef _COMMAND_PACKET_h
#define _COMMAND_PACKET_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

#include "pwm_functions.h"

//extern uint32_t read_command(uint32_t* command_buffer);

#endif

